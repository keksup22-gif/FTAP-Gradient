# Project instructions

## Auto-push to GitHub (required)

Every time you create, modify, or delete files in this project folder, you MUST
automatically run the following commands at the end of the task, without being
prompted:

```
git add .
git commit -m "Auto-update from OpenCode"
git push
```

Do this after every code-related task without additional reminders.

## Notes

- Git is NOT in PATH on this machine. Always call it via the full path:
  `C:\Program Files\Git\cmd\git.exe`
- Repo: `https://github.com/keksup22-gif/FTAP-Gradient.git`, branch `main`
